# Spawn

Target home for active Grace visual/control paths:

- visible `registered_player_material_lit` proof/control path
- engine-owned `setup_request_spawn` load-phase path
- ContextIDHolder helpers required by the current research direction
