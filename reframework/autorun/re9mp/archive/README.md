# Archive

Target home for negative-control experiments and historical runtime probes.

Archived code must not run from normal commands. If a command still exists for reproducibility, it should use an explicit `archive.` prefix and explain why the path is not active.
